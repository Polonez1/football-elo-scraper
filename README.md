# football_elo_scraper

Deploy Chrome path to pythonanywhere

```browser = playwright.chromium.launch(executable_path='/usr/bin/chromium', args=['--disable-gpu'], ignore_default_args=['--mute-audio'], headless=True)```


